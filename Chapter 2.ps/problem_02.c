// What data type will 3.0/8 − 2 return
#include <stdio.h>

int main()
{
    float a = (3.0 / 8) - 2;
    printf("The value of a is %f", a);
    return 0;
}
// It will return the output as -1.625 i.e. which is a float.