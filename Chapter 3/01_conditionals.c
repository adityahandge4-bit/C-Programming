#include <stdio.h>

int main()
{

    int age = 89;

    if (age > 10)
    {
        printf("The user is under the if \n");
        printf("The age of the person is %d", age);
    }

    else
    {
        printf("Yo are under age!");
    }

    return 0;
}