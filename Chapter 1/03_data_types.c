#include <stdio.h>

int main()
{
    // int a;
    // a=45;
    int a = 1;

    // float b = 1.4;
    float b;
    b = 8.9;

    // char c='a';
    char c;
    c = 'e';

    printf("The value of variable a is %d\n",a); //used for representing integers
    printf("The value of variable b is %f\n",b); // used for representing float number
    printf("The value of variable c is %c",c);  // used for representing character
}
