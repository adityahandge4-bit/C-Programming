#include<stdio.h>

int main(){

    char str[]="Aditya Sandeep Handge";
    // for (int i = 0; i <5; i++)
    // {
    //     printf("%c",str[i]);
    // }

    printf("%s",str); // Here %s is used for string as the marker and automatically as you have used the double quote so automatically the null character would be added
    
    
    return 0;
}